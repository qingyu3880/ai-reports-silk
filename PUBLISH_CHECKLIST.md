# 内容发布系统检查清单

## 核心理念
**推送到GitHub ≠ 部署成功**  
**文件生成 ≠ 用户收到**

必须建立完整的验证闭环。

---

## 发布流程验证清单

### 阶段1：生成与保存
- [ ] 内容已生成（符合格式要求）
- [ ] 文件已保存到指定路径
- [ ] 文件大小 > 1KB
- [ ] 文件内容完整（读取验证）

### 阶段2：网站同步
- [ ] sync_reports.py 执行成功
- [ ] reports.json 已更新（时间戳变化）
- [ ] GitHub 推送成功（有提交哈希）

### 阶段3：推送到对话（关键）
- [ ] **主代理**读取文件内容
- [ ] 使用 message 工具推送到对话
- [ ] 确认推送完成

### 阶段4：网站验证（关键）
- [ ] 等待 2 分钟（Vercel 部署时间）
- [ ] 使用 web_fetch 访问网站
- [ ] 验证网页包含最新内容标题
- [ ] 记录验证结果

---

## 错误处理规则

| 失败环节 | 处理方式 |
|---------|---------|
| 生成/保存失败 | 立即停止，不推送 |
| 同步失败 | 停止，检查文件权限 |
| GitHub推送失败 | 停止，检查Token和网络 |
| 对话推送失败 | 停止，重试或报告 |
| **网站验证失败** | **立即停止，报告错误，不标记完成** |

---

## 定时任务配置

### 主发布任务 (master-auto-publish)
- 时间：8:00, 9:00, 17:00, 20:00
- 流程：检查 → 生成 → 同步 → 推送 → 验证

### 同步任务 (auto-sync-reports)
- 时间：每6小时
- 流程：检查更新 → 同步 → 推送GitHub → 验证网站

---

## 常见错误与预防

### 错误1：推送到对话缺失
**原因**：依赖子代理推送，主代理未验证  
**预防**：主代理必须亲自读取文件并推送

### 错误2：网站未更新但任务标记完成
**原因**：推送到GitHub后未验证Vercel部署  
**预防**：必须 web_fetch 验证网页内容

### 错误3：重复推送
**原因**：未检查今天是否已有文件  
**预防**：每次执行前必须检查目录

---

## 验证命令参考

```bash
# 检查今天是否有文件
ls -la /root/.openclaw/workspace/recommendations/books/ | grep $(date +%Y-%m-%d)

# 验证GitHub提交
git log --oneline -1

# 验证reports.json更新
ls -lh /root/.openclaw/workspace/dashboard/reports.json
```

---

## 更新记录

- 2026-03-10：增加网站验证步骤，明确主代理推送责任
